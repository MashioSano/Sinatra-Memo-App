<button class="btn btn-primary d-inline-block" onclick="location.href='/memos/new'">メモを追加</button>
<div class=mt-3>
  <% @memos.each do |memo| %>
    <p class="title"><%= link_to("#{h memo.title}", "memos/#{memo.id}")%></p>
    <p><%= h memo.description %></p>
  <% end %>
</div>
