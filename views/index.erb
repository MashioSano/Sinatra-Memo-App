<button class="mt-3 btn btn-primary d-inline-block" onclick="location.href='/memos/new'">メモを追加</button>
<div class=mt-3>
  <% @memos.each do |id, values| %>
    <p class="title"><%= link_to("#{values["title"]}", "memos/#{id}")%></p>
    <p><%= values["description"] %></p>
  <% end %>
</div>
